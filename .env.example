# Docker / Railway environment configuration
# Copy to .env and customize before running docker-compose or deploying to Railway.

# Backend
DATABASE_URL=sqlite:///./data/hardware_hub.db
SECRET_KEY=change-me-in-production
OPENROUTER_API_KEY=
ADMIN_EMAIL=admin@booksy.com
ADMIN_PASSWORD=ChangeMe123!
CORS_ORIGINS=http://localhost:5173

# Frontend (used during build; defaults to http://localhost:8000)
VITE_API_URL=http://localhost:8000
